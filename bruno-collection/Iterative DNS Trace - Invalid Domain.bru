meta {
  name: Iterative DNS Trace - Invalid Domain
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/dns/trace
  body: json
  auth: none
}

body:json {
  {
    "domain": "google!.com",
    "type": "A"
  }
}

assert {
  res.status: eq 400
  res.body.error: contains not a valid domain name
}
