services:
  visualizer-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - API_PORT=4000
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173
